extends base.pug

block content
  section.section
    .container(class="is-flex is-align-items-center is-justify-content-center is-flex-direction-column")
      a.button.is-primary(href=`${authorizeURL}`) Click to Authorize App
